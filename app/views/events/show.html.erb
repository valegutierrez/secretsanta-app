<div class="row">
  <div class="col">
    <p id="notice"><%= notice %></p>

    <h2>
      <%= @event.title %>
    </h2>

    <p>
      <%= @event.description %>
    </p>

    <p>
      <strong>Members:</strong>
      <%= @event.members %>
    </p>

    <p>
      <strong>Price range:</strong>
      $<%= @event.price_start %> -
      $<%= @event.price_end %> CLP
    </p>

    <p>
      <strong>Deadline:</strong>
      <%= @event.deadline %>
    </p>
    <%= link_to 'Back', events_path, class: 'btn btn-danger' %>
    <%= link_to '' %>
    <br><br>
    <button type="button" class="btn btn-danger" data-toggle="popover" title="Popover title" data-content="And here's some amazing content. It's very engaging. Right?">Share Link</button>
  </div>

  <div class="col">
    <table class="table">
    <thead>
      <tr>
        <th scope="col"></th>
        <th scope="col">Member</th>
        <th scope="col">Status</th>
      </tr>
    </thead>

    <tbody>
      <% @event.attendances.each do |member| %>
        <tr>
          <% @users.each do |u| %>
            <% if member.user_id == u.id %>
              <td><%= image_tag u.image, height: 50, width: 50, class: 'rounded-lg'%></td>
              <td><%= u.name %></td>
            <% end %>
          <% end %>
            <% if member.user_id.nil? %>
            <td><%= image_tag "new-profile", height: 50, width: 50, class: 'rounded-lg' %></td>
            <td>Invitado</td>
            <td><div class="alert alert-secondary text-center">Pending</div></td>
            <% else %>
            <td><div class="alert alert-success text-center">Confirmed</div></td>
            <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>